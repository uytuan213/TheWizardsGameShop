@model IEnumerable<TheWizardsGameShop.Models.WizardsOrder>

@{
    ViewData["Title"] = "Manage Orders";
}

<div class="page contents" id="pageGames" --data-theme="employee-portal">
    <div class="page-header short flex-center-vertical">
        <h1><span class="material-icons">search</span>Search result for @ViewData["SearchKeyword"]</h1>
    </div>
    <div class="page-body">
        <div class="page-submenu">
            @*<a asp-controller="Home" asp-action="Index" class="item">&lt; Home</a>*@
        </div>
        <table class="table">
            <thead>
                <tr>
                    @*<th>
                        @Html.DisplayNameFor(model => model.CreditCard)
                    </th>*@
                    @*<th>
                        @Html.DisplayNameFor(model => model.MailingAddress)
                    </th>*@
                    <th>
                        @Html.DisplayNameFor(model => model.ShippingAddress)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Total)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderStatus)
                    </th>
                    <th>
            @Html.DisplayNameFor(model => model.User)
        </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    @*<td>
                        @item.CreditCard.CreditCardNumber.Substring(12, 4).PadLeft(16, '*')
                    </td>*@
                    @*<td>
                        @Html.DisplayFor(modelItem => item.MailingAddress.Street1) @Html.DisplayFor(modelItem => item.MailingAddress.Street2), @Html.DisplayFor(modelItem => item.MailingAddress.City)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.ShippingAddress.Street1) @Html.DisplayFor(modelItem => item.ShippingAddress.Street2), @Html.DisplayFor(modelItem => item.ShippingAddress.City), @Html.DisplayFor(modelItem => item.ShippingAddress.ProvinceCode) @Html.DisplayFor(modelItem => item.ShippingAddress.PostalCode)
                </td>
                    <td>
                        $@item.Total.ToString("N2")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderStatus.OrderStatus1)
                    </td>
                    <td>
            @Html.DisplayFor(modelItem => item.User.UserName)
        </td>
                    <td>
                        @*<a asp-action="Edit" asp-route-id="@item.OrderId">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.OrderId">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.OrderId">Delete</a>*@
                        <a asp-action="Complete" asp-route-id="@item.OrderId" class="btn btn--s">Mark Processed</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
        </div>
</div>