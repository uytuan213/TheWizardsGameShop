@model IEnumerable<TheWizardsGameShop.Models.Relationship>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsAccepted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsFamily)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReceiverNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SenderNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsAccepted)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsFamily)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceiverNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SenderNavigation.Email)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
